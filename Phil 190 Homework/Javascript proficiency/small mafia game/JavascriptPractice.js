console.log("test");


/*
//create a bot
var objA = new mysteryobjectsample();
//load role
for (var i = 0; i < role_loadfunctionlist.length; i++) {
	role_loadfunctionlist[i]("Hitman")(objA);
}
//test
objA.acquire_target();
console.log(objA.night_target);
objA.action_nact();
console.log(listofNPCs[objA.night_target[1]].deathdes);
*/
//test
/*
listofNPCs[0].acquire_target();
console.log(listofNPCs[0].night_target);
listofNPCs[0].action_nact();
console.log(listofNPCs[listofNPCs[0].night_target[1]].nametag
	+ listofNPCs[listofNPCs[0].night_target[1]].deathdes);
*/
console.log("test ends");









/*
document.write("<p>a for loop, a function, an array, an object, and does at least 5 other things </p><br><br>");


var number = Number( prompt("enter a number")); // typecast
var array = ["line1", "line2", number, "line4"];
var i = 0;
console.log(typeof(number) === 'number' && number != NaN);
if (typeof(number) === 'number' && number != NaN) {
	document.write("If/Then And Loop demostration.<br>");
	if (typeof(Object) != "number") {
		alert("You entered a " + typeof(number));
	}
	for (i = 0; i < array.length; i++) {
		array[i] = number + i;	
		document.write(array[i] + "contentchanged<br>");	
	}	
} 

if (number != 1) {
	document.write(number + " is reset to 1");
	number = 1;
	var mysterObj1 = new mysteryobjectsample ();
	for (prop in mysterObj1){
		console.log(prop);
	}
}

/*window.onload = function() {
    document.getElementById("menu").innerHTML = '<li class="topline"><a href="#">some text</a></li>  
                <li><a href="#">some text </a></li>  
                <li><a href="#">some text</a></li>  
                <li><a href="#">some text</a></li>  
                <li><a href="#"some text</a></li>  
                <li><a href="#">some text</a></li>  
                <li><a href="#">some text</a></li>';
}

function matrix( rows, cols, defaultValue){

  var arr = [];

  // Creates all lines:
  for(var i=0; i < rows; i++){

      // Creates an empty line
      arr.push([]);

      // Adds cols to the empty line:
      arr[i].push( new Array(cols));

      for(var j=0; j < cols; j++){
        // Initializes:
        arr[i][j] = defaultValue;
      }
  }

return arr;
}
*/